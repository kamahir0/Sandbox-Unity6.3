/* ============================================================
   Theme (t-) : 配色・フォント・視覚効果
   ============================================================ */

/* UIのルートに付与し、変数のスコープと文字色の継承を保証する */
.t-root {
    color: var(--color-window-text);
}

/* 実体のある「面」に適用し、背景色と文字色をセットで適用する */
.t-surface {
    background-color: var(--color-window-bg);
    color: var(--color-window-text);
}

/* ============================================================
   Layout (l-) : 構造・配置
   ============================================================ */

/* 画面いっぱいに広がるコンテナ */
.l-screen {
    width: 100%;
    height: 100%;
}

/* ウィンドウパネル構造 */
.l-window {
    border-radius: 12px;
    padding: 24px;
    border-width: 1px;
    border-color: var(--color-input-border);
    /* 簡易的な影 */
    border-bottom-width: 3px;
    flex-direction: column;
}

/* ============================================================
   Component (c-) : 個別のUIパーツ
   ============================================================ */

/* 共通の寸法設定 */
.c-control-size {
    height: var(--size-control-height);
    font-size: var(--font-size-standard);
    margin: var(--size-control-margin);
}

/* ボタンの基本スタイル */
.c-button {
    border-radius: 4px;
    border-width: 1px;
    -unity-font-style: bold;
    padding: 4px 12px;
}

/* 入力フィールドの基本スタイル */
.c-input .unity-base-field__label {
    -unity-text-align: middle-left;
    margin: 0;
    padding: 0;
}

/* スクロールビューの基本スタイル */
.c-scroll-view {
    flex-grow: 1;
}

/* ラベルの基本スタイル: c-control-size と併用し、高さを他のコントロールと合わせる */
.c-label {
    -unity-text-align: middle-left;
}

.c-input .unity-base-field__input {
    background-color: var(--color-input-bg);
    color: var(--color-input-text);
    border-color: var(--color-input-border);
    border-radius: 4px;
    border-width: 1px;
    padding: 0 8px;
    -unity-text-align: middle-left;
    --unity-cursor-color: var(--color-input-cursor);
}

/* --- セマンティック・カラー --- */
.c-button--primary {
    background-color: var(--color-primary-bg);
    color: var(--color-primary-text);
    border-color: var(--color-primary-border);
}

.c-button--secondary {
    background-color: var(--color-secondary-bg);
    color: var(--color-secondary-text);
    border-color: var(--color-secondary-border);
}

.c-button--danger {
    background-color: var(--color-danger-bg);
    color: var(--color-danger-text);
    border-color: var(--color-danger-border);
}

/* ============================================================
   Utility (u-) : 汎用的な調整
   ============================================================ */

/* 中央寄せ */
.u-center-content {
    justify-content: center;
    align-items: center;
}

/* 背景を完全に透明にする（オーバーレイ用） */
.u-bg-transparent {
    background-color: rgba(0, 0, 0, 0);
}

.c-foldout .unity-toggle .unity-toggle__checkmark {
    width: 54px;
    height: 54px;
}

/* Foldoutのラベル(text)のフォントサイズを標準に合わせる */
.c-foldout .unity-toggle__text {
    font-size: var(--font-size-standard);
    -unity-text-align: middle-left;
}

/* Foldout（閉じた状態のヘッダー部分）の基本スタイルをコントロール共通サイズに合わせる */
.c-foldout > .unity-foldout__toggle {
    margin: var(--size-control-margin);
    height: var(--size-control-height);
}
